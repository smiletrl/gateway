package constant

// logger
const (
	Logger = "logger"
)

// request
const (
	// request id
	RequestID string = "request_id"
	// request bdy
	RequestBody         string = "request_body"
	HttpRequestIDHeader        = "x-request-id"
)
